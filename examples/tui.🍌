kampung jefe

muak "boca"
muak "tubo_gordo"
muak "casa"
muak "bababas"

banana main() {
	pooka items [4]bababa
	items[0] = "inbox"
	items[1] = "agent"
	items[2] = "slackbot"
	items[3] = "quit"
	pooka selected me = 0
	pooka reader = tubo_gordo.NewReader(casa.Oreille)
	pooka line bababa

	boca.blabla("\033[2J\033[H")
	poopaye("Bello TUI demo")
	poopaye("commands: j=down, k=up, q=quit")
	poopaye("------")
	poopaye("  " + items[0])
	poopaye("  " + items[1])
	poopaye("  " + items[2])
	poopaye("  " + items[3])
	boca.blabla("> ")

	pooka input, err = reader.ReadString('\n')
	po err != hana {
		bapple
	}
	line = bababas.TrimSpace(input)

	po line == "j" {
		po selected < 3 {
			selected = selected + 1
		}
	} ka po line == "k" {
		po selected > 0 {
			selected = selected - 1
		}
	} ka po line == "q" {
		selected = 0
	}

	poopaye("---")
	poopaye("selected index:", selected)
}
