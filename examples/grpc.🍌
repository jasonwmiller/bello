kampung jefe

muak "google.golang.org/grpc"
muak "google.golang.org/grpc/credentials/insecure"

banana main() {
	pooka conn, err = grpc.Dial(
		"127.0.0.1:50051",
		grpc.WithTransportCredentials(insecure.NewCredentials()),
	)

	po err != hana {
		poopaye("grpc dial failed:", err)
		bapple
	}

	defer conn.Close()
	poopaye("grpc client created")
}
